# Server port (default is 8080, you can override if needed)
server.port=8080

# Logging level (optional, helps debug Firebase and HTTP calls)
logging.level.root=INFO
logging.level.com.example=DEBUG

# Firebase config
# We rely on GOOGLE_APPLICATION_CREDENTIALS env var to point to your service account JSON
# No need to hardcode here

# IPinfo token
# We rely on IPINFO_TOKEN env var, not stored in properties
